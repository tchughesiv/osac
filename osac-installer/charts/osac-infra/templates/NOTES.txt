OSAC shared infrastructure configured for release {{ .Release.Name }}.

Components:
{{- if .Values.certManager.enabled }}
  - CertManager CR
{{- end }}
{{- if .Values.trustManager.enabled }}
  - trust-manager certificates
{{- end }}
{{- if .Values.caIssuer.enabled }}
  - CA issuer and certificates
{{- end }}
{{- if and .Values.keycloak.enabled (eq .Values.keycloak.mode "managed") }}
  - Keycloak identity provider
{{- end }}
{{- if and .Values.keycloak.enabled (eq .Values.keycloak.mode "external") }}
  - OSAC realm imported into the external Keycloak identity provider
{{- end }}
{{- if .Values.lvms.enabled }}
  - LVMS configuration
{{- end }}
{{- if .Values.metallb.enabled }}
  - MetalLB configuration
{{- end }}
{{- if .Values.cnv.enabled }}
  - OpenShift Virtualization configuration
{{- end }}
{{- if .Values.mce.enabled }}
  - MultiCluster Engine configuration
{{- end }}
{{- if .Values.bundledPostgres.enabled }}
  - Bundled PostgreSQL (dev/CI)
{{- end }}
{{- if .Values.kafka.enabled }}
  - Kafka cluster (osac-kafka)
{{- end }}
